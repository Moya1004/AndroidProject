<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moya.chatapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Chat Application"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme.DayNight.DarkActionBar.edited"
        android:name=".ChatApp">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".Login"
            android:label="Login" />
        <activity
            android:name=".SignUp"
            android:label="Create Acount" />
        <activity android:name=".Home" />
        <activity
            android:name=".AcountSetting"
            android:label="Acount Settings" />
        <activity android:name=".All_Users"
            android:label="All Users"
            android:theme="@style/Widget.AppCompat.ActionBar.Solid"/>
        <activity android:name=".Profile"
            android:label="Profile"></activity>
    </application>

</manifest>